[project]
name = "judging-judges"
version = "0.1.0"
description = "A repo to evaluating judges for judge LLM solutions"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "litellm>=1.82.6",
    "pandas>=3.0.1",
    "pydantic>=2.12.5",
]
